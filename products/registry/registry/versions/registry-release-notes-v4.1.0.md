# Registry Release Notes - v4.1.0

Major Features introduced in 4.1.0

Intake Form is now treated as a separate route (INSERT) into a register. Change Requests is reserved for the EDIT route. All new records into a REGISTER will now be routed through Intake Forms. Intake Forms and Change Requests are now parallel routes into a REGISTER.
